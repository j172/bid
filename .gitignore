.env
.env.*.local
/.ssh/

# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts
tsconfig.tsbuildinfo

# local deploy scratch
/.prebuilt-next.tgz
/.prebuilt-public.tgz
/.ops-key

# runtime-uploaded listing photos (live only on the server)
/uploads/

# self-hosted TinyMCE runtime, copied from node_modules by postinstall
/public/tinymce/

# background-agent worktrees (each is its own separate checkout/.git)
/.claude/worktrees/
