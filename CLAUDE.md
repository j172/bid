# Claude Code project instructions

Read and follow [`AGENTS.md`](./AGENTS.md) before exploring or changing this
repository. It is the shared source of truth for project rules, commands,
security constraints, documentation wayfinding, and verification.

Claude-specific reminders:

- Read the most-specific applicable instruction file before editing.
- Never expose or modify secrets in `.env`.
- Keep changes scoped, inspect existing tests first, and run relevant checks
  before reporting completion.
- Preserve the generated Next.js rules block in `AGENTS.md`.